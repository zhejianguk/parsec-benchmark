PARSEC Benchmark Suite
======================

Quick Setup
-----------

    1. ./configure
    2. . env.sh
    3. pkgs/build_parsec.sh -t host      # or -t riscv
    4. pkgs/run_parsec.sh

Options
-------

build_parsec.sh:
  -t host    Build with host toolchain (default)
  -t riscv   Build with RISC-V toolchain
  -k gc_kernel   Specify GC kernel option

run_parsec.sh:
  -k gc_kernel   Specify GC kernel option

Benchmarks: blackscholes, bodytrack, dedup, ferret, fluidanimate, freqmine, streamcluster, swaptions
